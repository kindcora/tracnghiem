{
  "name": "quizmaster-pro",
  "version": "1.7.0",
  "description": "QuizMaster Pro — Vietnamese quiz PWA",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check script.js && node --check sw.js && node --check stats-worker.js"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
